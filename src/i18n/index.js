/**
 * 国际化入口文件
 * 统一导出所有消息
 * 
 * 使用示例：
 *   import { ROOM_MESSAGES, GAME_MESSAGES } from '@/i18n'
 *   showMessage(ROOM_MESSAGES.KICKED_OUT_TITLE)
 */

export * from './messages.js'

// 未来可以添加语言切换功能
// export function setLocale(locale) { ... }
// export function getLocale() { ... }

