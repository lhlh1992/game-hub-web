# 前端架构文档

## 目录

- [项目概述](#项目概述)
- [技术栈](#技术栈)
- [整体架构](#整体架构)
- [代码组织结构](#代码组织结构)
- [核心模块详解](#核心模块详解)
- [WebSocket 连接机制](#websocket-连接机制)
- [房间内逻辑](#房间内逻辑)
- [与后端交互](#与后端交互)
- [状态管理](#状态管理)
- [路由与页面](#路由与页面)
- [认证与授权](#认证与授权)
- [错误处理](#错误处理)
- [性能优化](#性能优化)
- [十二、WebSocket 连接机制详解](#十二websocket-连接机制详解)
  - [12.1 Game WebSocket（gomokuSocket.js）](#121-game-websocketgomokusocketjs)
  - [12.2 Chat WebSocket（chatSocket.js）](#122-chat-websocketchatsocketjs)
- [十三、好友申请机制](#十三好友申请机制)
- [十四、消息通知机制](#十四消息通知机制)
- [十五、私聊机制](#十五私聊机制)
- [十六、房间内会话机制](#十六房间内会话机制)
- [十七、前后端交互 API 清单](#十七前后端交互-api-清单)

---

## 项目概述

Game Hub Web 是一个基于 React 的实时五子棋游戏前端应用，采用单页应用（SPA）架构，通过 WebSocket 实现实时游戏逻辑和聊天功能。应用支持玩家对战（PVP）和人机对战（PVE）两种模式，提供完整的房间管理、实时对局、聊天通信（含历史回溯）、好友系统、私聊、消息通知等功能。

### 核心特性

- **实时游戏对局**：基于 WebSocket 的实时棋盘状态同步
- **双 WebSocket 连接**：游戏逻辑与聊天功能独立连接，互不干扰
- **房间聊天历史**：进入房间自动加载最近 50 条历史（Redis 24h TTL，按房间隔离）
- **私聊会话**：支持好友间私聊，包含已读未读状态、未读计数、历史消息加载
- **好友系统**：申请加好友、同意/拒绝申请、好友列表管理、自动刷新机制
- **消息通知**：WebSocket 实时推送通知（好友申请、系统通知等），支持未读计数
- **自动重连机制**：连接断开时自动重连，保证用户体验
- **心跳保活**：5 秒心跳间隔，及时检测连接状态
- **房间管理**：创建、加入、离开房间，支持房主权限
- **用户认证**：基于 Keycloak 的 OAuth2 认证流程

---

## 技术栈

### 核心框架

- **React 19.2.0**：UI 框架
- **React Router 7.9.6**：路由管理
- **Vite 7.2.4**：构建工具和开发服务器

### WebSocket 通信

- **SockJS 1.6.1**：WebSocket 传输层，提供降级方案
- **@stomp/stompjs 7.2.1**：STOMP 协议客户端，用于结构化消息传递

### 开发工具

- **ESLint**：代码规范检查
- **React Hooks**：函数式组件状态管理

---

## 整体架构

### 架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        浏览器客户端                           │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌─────────────────────────────────────────────────────┐    │
│  │              React 应用层 (SPA)                      │    │
│  ├─────────────────────────────────────────────────────┤    │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐          │    │
│  │  │  Pages   │  │Components│  │  Hooks   │          │    │
│  │  └──────────┘  └──────────┘  └──────────┘          │    │
│  │       │             │             │                   │    │
│  │       └─────────────┼─────────────┘                   │    │
│  │                     │                                 │    │
│  │  ┌──────────────────────────────────────┐           │    │
│  │  │         Contexts (状态管理)           │           │    │
│  │  │  - AuthContext (main.jsx)            │           │    │
│  │  │  - OngoingGameContext (AppLayout)    │           │    │
│  │  └──────────────────────────────────────┘           │    │
│  └─────────────────────────────────────────────────────┘    │
│                     │                                        │
│  ┌──────────────────┼──────────────────┐                     │
│  │                  │                  │                     │
│  ┌──────────────┐  │  ┌──────────────┐                    │
│  │  Services    │  │  │  WebSocket   │                    │
│  │  Layer       │  │  │  Layer       │                    │
│  ├──────────────┤  │  ├──────────────┤                    │
│  │ - apiClient  │  │  │ - gomokuSocket│                   │
│  │ - gameApi    │  │  │ - chatSocket │                   │
│  │ - userApi    │  │  │              │                   │
│  │ - chatApi    │  │  │              │                   │
│  │ - friendApi  │  │  │              │                   │
│  │ - notificationApi│ │              │                   │
│  │ - sessionMonitor│ │              │                   │
│  │ - authService│  │  │              │                   │
│  └──────────────┘  │  └──────────────┘                    │
│                     │                  │                     │
└─────────────────────┼──────────────────┼─────────────────────┘
                      │                  │
         ┌────────────┘                  └────────────┐
         │                                           │
    ┌────▼─────┐                              ┌─────▼────┐
    │  HTTP    │                              │ WebSocket│
    │  API     │                              │  (STOMP) │
    └────┬─────┘                              └─────┬────┘
         │                                         │
         └─────────────────┬───────────────────────┘
                           │
                    ┌──────▼──────┐
                    │   Gateway   │
                    │  (Backend)  │
                    └─────────────┘
```

### 分层说明

1. **页面层（Pages）**：路由对应的页面组件，负责页面级状态和布局
2. **组件层（Components）**：可复用的 UI 组件
3. **Hooks 层**：业务逻辑封装，提供状态和副作用管理
4. **服务层（Services）**：API 调用和 WebSocket 连接管理
5. **上下文层（Contexts）**：全局状态管理

---

## 代码组织结构

```
src/
├── main.jsx                    # 应用入口
├── App.jsx                     # 根组件，路由配置
├── components/                 # 可复用组件
│   ├── chat/
│   │   └── GlobalChat.jsx     # 全局聊天组件（私聊、好友列表）
│   ├── common/
│   │   └── ProtectedRoute.jsx # 路由保护组件
│   ├── layout/
│   │   ├── AppLayout.jsx      # 应用布局
│   │   └── Header.jsx         # 顶部导航栏
│   └── profile/
│       ├── ProfileHome.jsx    # 个人中心首页
│       ├── ProfileEdit.jsx    # 个人资料编辑
│       └── GameCard.jsx       # 游戏卡片组件
├── pages/                      # 页面组件
│   ├── HomePage.jsx           # 首页
│   ├── LobbyPage.jsx          # 大厅页面
│   ├── GameRoomPage.jsx       # 游戏房间页面（核心）
│   ├── ProfilePage.jsx        # 个人中心
│   ├── MessageCenterPage.jsx # 消息中心（通知列表）
│   ├── SessionMonitorPage.jsx # 会话监控
│   └── NotFoundPage.jsx       # 404 页面
├── hooks/                      # 自定义 Hooks
│   ├── useAuth.js             # 认证 Hook（重新导出）
│   ├── useGomokuGame.js       # 游戏逻辑 Hook
│   ├── useChatRoomWs.js       # 房间聊天 WebSocket Hook
│   ├── useGlobalChatWs.js     # 全局聊天 WebSocket Hook
│   └── useOngoingGame.js      # 进行中对局 Hook
├── contexts/                   # React Context
│   ├── AuthContext.jsx        # 认证上下文
│   └── OngoingGameContext.jsx # 进行中对局上下文
├── services/                   # 服务层
│   ├── api/                    # HTTP API 客户端
│   │   ├── apiClient.js       # 统一请求封装
│   │   ├── gameApi.js         # 游戏相关 API
│   │   ├── userApi.js         # 用户相关 API
│   │   ├── chatApi.js         # 聊天相关 API
│   │   ├── friendApi.js       # 好友相关 API
│   │   ├── notificationApi.js  # 通知相关 API
│   │   └── sessionMonitor.js  # 会话监控 API
│   ├── auth/                   # 认证服务
│   │   └── authService.js     # Token 管理和认证流程
│   └── ws/                     # WebSocket 客户端
│       ├── gomokuSocket.js    # 游戏 WebSocket
│       └── chatSocket.js      # 聊天 WebSocket
├── config/                     # 配置文件
│   └── appConfig.js           # 应用配置
├── constants/                  # 常量定义
│   ├── gameConstants.js       # 游戏常量
│   └── gameEnums.js           # 游戏枚举
├── i18n/                       # 国际化
│   ├── index.js               # 导出
│   └── messages.js            # 消息定义
└── styles/                     # 样式文件
    ├── global.css             # 全局样式
    ├── game.css               # 游戏页面样式
    ├── lobby.css              # 大厅样式
    └── ...
```

---

## 核心模块详解

### 1. 认证模块（AuthContext & authService）

#### AuthContext

提供全局认证状态管理，包括：

- `isAuthenticated`：是否已认证
- `isLoading`：认证状态加载中
- `user`：当前用户信息
- `login()`：触发登录流程
- `logout()`：登出
- `refreshUser()`：刷新用户信息

#### authService

负责 Token 管理和认证流程：

- **Token 存储**：使用 `localStorage` 存储 `access_token`
- **Token 获取**：优先从 Gateway `/token` 接口获取（支持自动刷新），失败则使用本地存储
- **Token 验证**：通过 `/game-service/me` 接口验证 Token 有效性
- **认证流程**：
  1. 检查本地 Token → 验证有效性
  2. 无效则从 Gateway 获取新 Token
  3. 仍无效则跳转 Keycloak 登录页
- **401 处理**：检测到 401 响应时，显示登录失效弹窗，引导用户重新登录

### 2. WebSocket 模块

详见 [WebSocket 连接机制](#websocket-连接机制) 章节。

### 3. 游戏逻辑模块（useGomokuGame）

`useGomokuGame` 是游戏房间的核心 Hook，负责：

- **游戏状态管理**：棋盘、回合、胜负、倒计时等
- **WebSocket 订阅**：房间事件、座位分配、完整同步、被踢事件
- **游戏操作**：落子、认输、重新开始、准备、开始游戏
- **房间信息**：房间阶段、房主信息、座位信息、玩家连接状态

#### 状态同步机制

1. **首屏加载**：通过 HTTP `getRoomView()` 获取房间快照，确保即使 WebSocket 未连接也能显示房间状态
2. **WebSocket 连接后**：立即再次获取快照，确保获取最新状态
3. **实时更新**：通过 STOMP 订阅接收房间事件（`STATE`、`SNAPSHOT`、`TICK` 等）

#### 事件处理

- `STATE`：游戏状态更新（棋盘、回合、胜负）
- `SNAPSHOT`：房间完整快照（座位、准备状态、阶段等）
- `TICK`：倒计时更新
- `ERROR`：错误消息（如禁手提示）
- `READY_STATUS`、`ROOM_STATUS`：已废弃，统一使用 `SNAPSHOT`

---

## WebSocket 连接机制

### 架构设计

应用采用**双 WebSocket 连接**架构，游戏逻辑和聊天功能完全独立：

```
┌─────────────────────────────────────────────────┐
│            GameRoomPage (游戏房间页面)            │
├─────────────────────────────────────────────────┤
│                                                  │
│  ┌──────────────────────────────────────────┐   │
│  │  useGomokuGame Hook                     │   │
│  │  ┌────────────────────────────────────┐ │   │
│  │  │  gomokuSocket.js                  │ │   │
│  │  │  - 连接: /game-service/ws         │ │   │
│  │  │  - 用途: 游戏逻辑                  │ │   │
│  │  │  - 订阅:                           │ │   │
│  │  │    • /topic/room.{roomId}          │ │   │
│  │  │    • /user/queue/gomoku.seat      │ │   │
│  │  │    • /user/queue/gomoku.full      │ │   │
│  │  │    • /user/queue/gomoku.kicked    │ │   │
│  │  │  - 发送:                           │ │   │
│  │  │    • /app/gomoku.resume           │ │   │
│  │  │    • /app/gomoku.place            │ │   │
│  │  │    • /app/gomoku.ready            │ │   │
│  │  │    • ...                           │ │   │
│  │  └────────────────────────────────────┘ │   │
│  └──────────────────────────────────────────┘   │
│                                                  │
│  ┌──────────────────────────────────────────┐   │
│  │  useChatRoomWs Hook                     │   │
│  │  ┌────────────────────────────────────┐ │   │
│  │  │  chatSocket.js                     │ │   │
│  │  │  - 连接: /chat-service/ws          │ │   │
│  │  │  - 用途: 房间聊天                   │ │   │
│  │  │  - 订阅:                            │ │   │
│  │  │    • /topic/chat.room.{roomId}     │ │   │
│  │  │  - 发送:                            │ │   │
│  │  │    • /app/chat.room.send           │ │   │
│  │  └────────────────────────────────────┘ │   │
│  └──────────────────────────────────────────┘   │
│                                                  │
│  ┌──────────────────────────────────────────┐   │
│  │  AppLayout (全局布局)                    │   │
│  │  ┌────────────────────────────────────┐ │   │
│  │  │  useGlobalChatWs Hook              │ │   │
│  │  │  - 共享 chatSocket.js 连接          │ │   │
│  │  │  - 用途: 全局聊天连接管理            │ │   │
│  │  │  - 监听: onNotify (广播 gh-notify)  │ │   │
│  │  └────────────────────────────────────┘ │   │
│  │  ┌────────────────────────────────────┐ │   │
│  │  │  GlobalChat 组件                   │ │   │
│  │  │  - 私聊、好友列表                   │ │   │
│  │  │  - 订阅私聊消息                     │ │   │
│  │  │  - 监听好友申请结果通知             │ │   │
│  │  └────────────────────────────────────┘ │   │
│  │  ┌────────────────────────────────────┐ │   │
│  │  │  OngoingGameProvider                │ │   │
│  │  │  - 进行中对局状态管理               │ │   │
│  │  └────────────────────────────────────┘ │   │
│  └──────────────────────────────────────────┘   │
└─────────────────────────────────────────────────┘
```

### 连接独立性

两条 WebSocket 连接**完全独立**：

1. **不同的服务端点**：
   - Game WS：`/game-service/ws`
   - Chat WS：`/chat-service/ws`

2. **独立的连接管理**：
   - 各自维护 `socket`、`stomp`、`subscriptions` 等状态
   - 各自的重连逻辑和计数器
   - 各自的心跳检测

3. **独立的生命周期**：
   - Game WS：进入房间时连接，离开房间时断开
   - Chat WS：登录后全局连接，登出时断开

4. **互不影响**：
   - 一条连接断开不影响另一条
   - 一条连接重连不影响另一条
   - 各自的状态提示独立显示

### 连接建立流程

#### Game WebSocket（gomokuSocket.js）

```javascript
connectWebSocket(callbacks)
  ↓
connectWebSocketInternal(callbacks, isInitialConnect)
  ↓
1. 检查是否已连接 → 是则直接回调 onConnect
2. 获取 Token（ensureAuthenticated）
3. 创建 SockJS 连接：/game-service/ws?access_token={token}
4. 创建 STOMP 客户端（Stomp.over(socket)）
5. 配置心跳：outgoing=5000ms, incoming=5000ms
6. 连接 STOMP（带 Authorization header）
7. 连接成功后：
   - 启动心跳检测定时器（每 5 秒检查 stomp.connected）
   - 回调 onConnect
8. 订阅房间事件（由 useGomokuGame 调用）
```

#### Chat WebSocket（chatSocket.js）

```javascript
connectChatWebSocket(callbacks)
  ↓
1. 添加回调到 callbackListeners Set（支持多个监听器）
2. 检查是否已连接 → 是则直接回调新监听器的 onConnect
3. 检查是否正在重连 → 是则只添加监听器，不干扰重连
4. connectChatWebSocketInternal(isInitialConnect)
  ↓
（后续流程与 Game WS 类似，但连接地址为 /chat-service/ws）
```

### 心跳机制

#### 配置

- **心跳间隔**：5 秒（客户端和服务端双向）
- **检测频率**：每 5 秒检查一次 `stomp.connected` 状态

#### 实现细节

**后端配置**（Spring WebSocket）：
```java
registry.enableSimpleBroker("/topic", "/queue")
    .setHeartbeatValue(new long[]{5000, 5000})  // [客户端发送间隔, 服务端发送间隔]
    .setTaskScheduler(wsHeartbeatTaskScheduler)
```

**前端配置**（STOMP 客户端）：
```javascript
stomp.heartbeat.outgoing = 5000  // 客户端每 5 秒发送心跳
stomp.heartbeat.incoming = 5000  // 期望服务端每 5 秒发送心跳
```

**主动检测**：
```javascript
// 连接成功后启动定期检查
heartbeatCheckInterval = setInterval(() => {
  if (stomp && !stomp.connected) {
    // 检测到断开，触发重连
    callbacks.onDisconnect?.()
    scheduleReconnect(false)
  }
}, 5000)
```

### 自动重连机制

#### 重连策略

采用**指数退避**策略：

- **初始延迟**：1 秒
- **最大延迟**：30 秒
- **最大重连次数**：10 次
- **延迟计算**：`min(1000 * 2^attempt, 30000)`

重连延迟序列：1s → 2s → 4s → 8s → 16s → 30s（上限）→ 30s → ...

#### 重连触发条件

1. **连接关闭**：`socket.onclose` 事件
2. **连接错误**：`socket.onerror` 事件（SockJS 检测到错误）
3. **STOMP 错误**：`stomp.onStompError` 回调
4. **连接超时**：10 秒内未连接成功
5. **心跳检测失败**：定期检查发现 `stomp.connected === false`

#### 重连保护机制

1. **手动断开不重连**：`isManualDisconnect` 标志，登出或离开房间时设置为 `true`
2. **401 不重连**：检测到未授权错误时，直接触发登出流程
3. **初始连接不显示提示**：`isInitialConnect` 参数，避免首次连接时显示重连提示

#### 重连流程

```javascript
scheduleReconnect(isInitialConnect)
  ↓
1. 检查 isManualDisconnect → 是则退出
2. 检查重连次数 → 达到上限则回调 onReconnectFailed
3. 计算延迟（指数退避）
4. 如果不是初始连接，回调 onReconnecting（显示提示）
5. 设置定时器，延迟后调用 connectWebSocketInternal
  ↓
connectWebSocketInternal
  ↓
（重新建立连接流程）
```

### 多监听器支持（Chat WebSocket）

Chat WebSocket 支持多个组件同时监听同一连接：

#### 问题背景

在游戏房间页面中，`useGlobalChatWs`（全局布局）和 `useChatRoomWs`（房间页面）都需要监听 Chat WebSocket 连接状态。如果使用单个回调变量，后调用的会覆盖先调用的，导致只有最后一个组件能收到重连事件。

#### 解决方案

使用 `Set` 存储多个回调对象：

```javascript
// chatSocket.js
const callbackListeners = new Set()

function notifyListeners(method, ...args) {
  callbackListeners.forEach((callbacks) => {
    if (callbacks && typeof callbacks[method] === 'function') {
      try {
        callbacks[method](...args)
      } catch (error) {
        // 监听器回调出错，不影响其他监听器
      }
  })
}

export async function connectChatWebSocket(callbacks = {}) {
  // 添加回调到 Set
  if (callbacks && Object.keys(callbacks).length > 0) {
    callbackListeners.add(callbacks)
  }
  
  // 如果已连接，直接通知新监听器
  if (stomp && stomp.connected) {
    callbacks.onConnect?.()
    return
  }
  
  // 如果正在重连，只添加监听器，不干扰重连
  if (reconnectTimer) {
    return
  }
  
  // 初始化连接
  await connectChatWebSocketInternal(true)
}

export function removeChatWebSocketCallbacks(callbacks) {
  callbackListeners.delete(callbacks)
}
```

#### 使用方式

```javascript
// useGlobalChatWs.js
useEffect(() => {
  const callbacks = { onConnect, onDisconnect, ... }
  connectChatWebSocket(callbacks)
  return () => {
    removeChatWebSocketCallbacks(callbacks)  // 只移除自己的回调
  }
}, [isAuthenticated])

// useChatRoomWs.js
useEffect(() => {
  const callbacks = { onConnect, onDisconnect, ... }
  connectChatWebSocket(callbacks)
  return () => {
    removeChatWebSocketCallbacks(callbacks)  // 只移除自己的回调
  }
}, [roomId, onMessage])
```

### STOMP 订阅管理

#### 订阅结构

使用 `Map` 存储订阅，key 为 topic，value 为订阅对象：

```javascript
const subscriptions = new Map()

function subscribeRoom(roomId, onEvent) {
  const topic = `/topic/room.${roomId}`
  
  // 如果已订阅，先取消旧订阅
  if (subscriptions.has(topic)) {
    subscriptions.get(topic).unsubscribe()
  }
  
  // 创建新订阅
  const sub = client.subscribe(topic, (frame) => {
    const evt = JSON.parse(frame.body)
    onEvent(evt)
  })
  
  subscriptions.set(topic, sub)
}
```

#### 订阅清理

- **手动断开**：`disconnectWebSocket()` 时清理所有订阅
- **组件卸载**：Hook 的 `useEffect` cleanup 函数中取消特定订阅

---

## 房间内逻辑

### 页面结构

`GameRoomPage` 是游戏房间的核心页面，包含：

1. **游戏棋盘**：15x15 五子棋棋盘
2. **玩家面板**：显示自己和对手的信息、准备状态、连接状态
3. **聊天面板**：房间内聊天消息
4. **状态栏**：显示当前回合、倒计时、游戏状态
5. **操作按钮**：准备、开始游戏、认输、重新开始等

### 双 WebSocket 连接在房间内的协作

#### Game WebSocket（useGomokuGame）

**职责**：
- 游戏逻辑：落子、准备、开始游戏、认输、重新开始
- 房间状态：座位分配、房间阶段、玩家连接状态
- 游戏状态：棋盘、回合、胜负、倒计时

**订阅**：
- `/topic/room.{roomId}`：房间事件（STATE、SNAPSHOT、TICK、ERROR 等）
- `/user/queue/gomoku.seat`：座位分配（seatKey、side）
- `/user/queue/gomoku.full`：完整同步（重连时）
- `/user/queue/gomoku.kicked`：被踢事件（房间内被房主踢出，通过 `subscribeKicked()` 订阅）
- `/user/queue/system.kick`：系统踢线通知（单设备登录/登出等，连接时自动订阅，收到后停止重连）

**发送**：
- `/app/gomoku.resume`：恢复连接
- `/app/gomoku.place`：落子
- `/app/gomoku.ready`：准备/取消准备
- `/app/gomoku.start`：开始游戏
- `/app/gomoku.resign`：认输
- `/app/gomoku.restart`：重新开始
- `/app/gomoku.kick`：踢出玩家

#### Chat WebSocket（useChatRoomWs）

**职责**：
- 房间内聊天消息收发
- 进入房间时，`GameRoomPage` 先通过 HTTP 拉取最近 50 条聊天历史，然后 `useChatRoomWs` 订阅实时消息

**订阅**：
- `/topic/chat.room.{roomId}`：房间聊天消息（广播）

**发送**：
- `/app/chat.room.send`：发送聊天消息

**注意**：`useChatRoomWs` 只负责房间聊天，私聊消息由 `GlobalChat` 组件通过 `subscribePrivateChat` 订阅

### 状态同步流程

#### 1. 进入房间

```
用户进入 /game/:roomId
  ↓
GameRoomPage 组件挂载
  ↓
useGomokuGame Hook 初始化
  ↓
1. HTTP 请求：getRoomView(roomId) → 获取房间快照（首屏渲染）
2. WebSocket 连接：connectWebSocket() → 建立 Game WS 连接
3. WebSocket 连接成功后：
   - 订阅房间事件
   - 订阅座位分配
   - 订阅完整同步
   - 订阅被踢事件
   - 发送 resume 请求
   - 再次 HTTP 请求：getRoomView(roomId) → 确保获取最新状态
  ↓
useChatRoomWs Hook 初始化
  ↓
先拉取房间聊天历史：GET /chat-service/api/rooms/{roomId}/history?limit=50
  ↓
connectChatWebSocket() → 建立 Chat WS 连接（或复用已有连接）
  ↓
订阅房间聊天：subscribeRoomChat(roomId, onMessage)
```

#### 2. 游戏进行中

```
玩家落子
  ↓
placeStone(x, y) → sendPlace(roomId, x, y, side, seatKey)
  ↓
Game WS 发送：/app/gomoku.place
  ↓
后端处理，广播 STATE 事件
  ↓
前端接收：/topic/room.{roomId} → handleRoomEvent(evt)
  ↓
更新棋盘状态：setBoard(newBoard)
更新回合：setSideToMove(newSide)
更新倒计时：setCountdown(tick)
```

#### 3. 连接断开与重连

```
网络断开 / 后端重启
  ↓
Game WS：socket.onclose 触发
  ↓
scheduleReconnect(false) → 显示重连提示
  ↓
指数退避延迟后：connectWebSocketInternal()
  ↓
重新建立连接
  ↓
连接成功后：
  - 重新订阅所有 topic
  - 发送 resume 请求（携带 seatKey）
  - 后端返回完整同步（/user/queue/gomoku.full）
  ↓
handleFullSync(snap) → 恢复所有游戏状态
```

**Chat WS 重连流程类似，但独立进行，不影响 Game WS。**
**被踢处理**：游戏 WS 收到被踢事件只弹出“被踢出房间”提示并跳转大厅，不触发登录失效；同时刷新进行中对局状态以清空残留。

### 用户信息缓存机制

#### 问题背景

聊天消息中可能只包含 `senderId`，没有 `senderName`。如果每次都请求后端获取用户信息，会导致频繁的 API 调用。

#### 解决方案

使用本地缓存 `userInfoCache`，优先级策略：

1. **消息中的 senderName**（最高优先级）
2. **本地缓存**（`userInfoCache[userId]`）
3. **座位信息**（`seatXUserInfo`、`seatOUserInfo`）
4. **senderId**（兜底）

#### 缓存更新时机

1. **进入房间时**：从房间快照中提取座位用户信息，写入缓存
2. **收到消息时**：如果消息包含用户信息，更新缓存
3. **懒加载**：如果消息只有 `senderId` 没有 `senderName`，且缓存中没有，则请求后端获取
4. **用户信息更新**：当用户在个人中心修改信息后，`useAuth` 的 `user` 对象更新，同步更新本地缓存

#### 实现细节

```javascript
// GameRoomPage.jsx
const [userInfoCache, setUserInfoCache] = useState({})

const resolveDisplayName = useCallback((senderId, senderName) => {
  if (senderName) return senderName
  
  const key = String(senderId)
  const cachedProfile = userInfoCache[key]
  if (cachedProfile) {
    return cachedProfile.nickname || cachedProfile.username || key
  }
  
  // 兜底：查座位信息
  const candidates = [seatXUserInfo, seatOUserInfo]
  for (const info of candidates) {
    if (info?.userId === key || info?.systemUserId === key) {
      return info.nickname || info.username || key
    }
  }
  
  return key || 'Unknown'
}, [userInfoCache, seatXUserInfo, seatOUserInfo])

// 懒加载用户信息
const ensureUserProfile = useCallback(async (userId) => {
  if (pendingUserFetch.current.has(userId)) return
  
  pendingUserFetch.current.add(userId)
  try {
    const info = await getUserInfo(userId)
    if (info) {
      upsertUserInfos([info])  // 更新缓存
      // 更新已收到的消息显示名
      setChatHistory((prev) => prev.map(msg => 
        msg.senderId === userId 
          ? { ...msg, senderName: resolveDisplayName(userId, null) }
          : msg
      ))
    }
  } finally {
    pendingUserFetch.current.delete(userId)
  }
}, [])
```

---

## 与后端交互

### HTTP API

#### API 客户端封装（apiClient.js）

统一封装所有 HTTP 请求：

- **认证处理**：自动添加 `Authorization: Bearer {token}` header
- **401 处理**：检测到 401 时触发登出流程（调用 `handleAuthExpiredResponse`）
- **错误处理**：统一解析错误响应，提取 `message` 字段

**主要函数**：
- `authenticatedFetch(url, options)`：带认证的 fetch 请求
- `authenticatedJsonFetch(url, options)`：带认证的 JSON 请求（自动处理 JSON 响应）
- `get(url, options)`：GET 请求
- `post(url, data, options)`：POST 请求
- `put(url, data, options)`：PUT 请求
- `del(url, options)`：DELETE 请求（虽然定义，但当前代码中未使用）

#### 主要 API 接口

**游戏相关**（gameApi.js）：
- `createRoom(options)`：创建房间
- `joinRoom(roomId)`：加入房间
- `leaveRoom(roomId)`：离开房间
- `getRoomView(roomId)`：获取房间快照
- `getOngoingGame()`：获取进行中的对局
- `endOngoingGame(roomId)`：结束对局
- `listGomokuRooms(params)`：获取房间列表
- `getUserInfos(userIds)`：批量获取用户信息
- `getUserInfo(userId)`：获取单个用户信息

**用户相关**（userApi.js）：
- `getCurrentUserProfile()`：获取用户资料
- `updateUserProfile(data)`：更新用户资料
- `uploadAvatarToTemp(file)`：上传头像到临时目录

**聊天相关**（chatApi.js）：
- `getRoomChatHistory(roomId, limit)`：获取房间聊天历史

**好友相关**（friendApi.js）：
- `applyFriend(targetUserId, requestMessage)`：申请加好友
- `acceptFriendRequest(requestId)`：同意好友申请
- `rejectFriendRequest(requestId)`：拒绝好友申请
- `getFriendsList()`：获取好友列表

**通知相关**（notificationApi.js）：
- `fetchNotifications(status, limit)`：获取通知列表
- `fetchUnreadCount()`：获取未读计数
- `markNotificationRead(id)`：标记通知已读
- `markAllNotificationsRead()`：标记全部已读

**认证相关**（authService.js）：
- `getTokenFromGateway()`：从 Gateway 获取 Token
- `ensureAuthenticated(autoLogin)`：确保已认证（优先 Gateway `/token`，失败则使用本地存储）
- `performSessionLogout(message)`：执行登出流程（显示提示，清理 Token，断开 WebSocket）

### WebSocket API

#### Game WebSocket（game-service）

**订阅（接收）**：
- `/topic/room.{roomId}`：房间事件
- `/user/queue/gomoku.seat`：座位分配
- `/user/queue/gomoku.full`：完整同步
- `/user/queue/gomoku.kicked`：被踢事件

**发送**：
- `/app/gomoku.resume`：恢复连接
- `/app/gomoku.place`：落子
- `/app/gomoku.ready`：准备/取消准备
- `/app/gomoku.start`：开始游戏
- `/app/gomoku.resign`：认输
- `/app/gomoku.restart`：重新开始
- `/app/gomoku.kick`：踢出玩家

#### Chat WebSocket（chat-service）

**订阅（接收）**：
- `/topic/chat.room.{roomId}`：房间聊天消息（广播）
- `/user/queue/chat.private`：私聊消息（点对点，Spring 自动路由）
- `/user/queue/notify`：用户通知（点对点，Spring 自动路由）
- `/user/queue/system.kick`：系统踢线通知（点对点，Spring 自动路由）

**发送**：
- `/app/chat.room.send`：发送房间消息
- `/app/chat.private.send`：发送私聊消息

### 认证流程

#### Token 传递

**HTTP 请求**：
```
Authorization: Bearer {token}
```

**WebSocket 连接**：
1. **URL 参数**：`/game-service/ws?access_token={token}`（SockJS 握手请求）
2. **STOMP Header**：`Authorization: Bearer {token}`（STOMP 连接请求）

#### Token 刷新

Gateway 的 `/token` 接口支持自动刷新 Token（基于 Cookie），前端优先使用该接口获取最新 Token。

---

## 状态管理

### Context API

#### AuthContext

全局认证状态：

```javascript
{
  isAuthenticated: boolean,
  isLoading: boolean,
  user: UserInfo | null,
  login: () => void,
  logout: () => void,
  refreshUser: () => Promise<void>
}
```

#### OngoingGameContext

进行中对局状态：

```javascript
{
  loading: boolean,
  data: { hasOngoing: boolean, roomId?: string, title?: string, ... } | null,
  error: Error | null,
  refresh: () => Promise<void>,
  end: (roomId?: string) => Promise<void>
}
```

**实现细节**：
- **Provider**：`OngoingGameProvider` 在应用启动时自动调用 `getOngoingGame()` 获取进行中的对局
- **刷新机制**：调用 `refresh()` 会重新调用 `getOngoingGame()` API
- **结束对局**：调用 `end(roomId)` 会调用 `endOngoingGame(roomId)` API，成功后清空 `data`
- **使用方式**：通过 `useOngoingGame()` Hook 访问（实际是 `useOngoingGameContext()` 的重新导出）

### 组件级状态

使用 `useState` 管理组件内部状态，如：

- `GameRoomPage`：聊天历史、用户信息缓存、UI 状态（弹窗、提示等）
- `useGomokuGame`：游戏状态（棋盘、回合、胜负等）
- `useChatRoomWs`：连接状态（connected、reconnecting、error）

### 状态同步策略

1. **首屏加载**：HTTP 请求获取初始状态
2. **WebSocket 连接后**：立即获取最新状态（HTTP）
3. **实时更新**：WebSocket 事件驱动状态更新
4. **重连恢复**：通过完整同步（FullSync）恢复状态

---

## 路由与页面

### 路由配置（App.jsx）

```javascript
<Routes>
  <Route element={<AppLayout />}>
    <Route path="/" element={<HomePage />} />
    <Route path="/lobby" element={<ProtectedRoute><LobbyPage /></ProtectedRoute>} />
    <Route path="/game/:roomId" element={<ProtectedRoute><GameRoomPage /></ProtectedRoute>} />
    <Route path="/profile" element={<ProtectedRoute><ProfilePage /></ProtectedRoute>} />
    <Route path="/messages" element={<ProtectedRoute><MessageCenterPage /></ProtectedRoute>} />
    <Route path="/sessions" element={<SessionMonitorPage />} />
    <Route path="*" element={<NotFoundPage />} />
  </Route>
</Routes>
```

### 页面说明

1. **HomePage**：首页，未登录用户入口
2. **LobbyPage**：大厅，显示房间列表，支持：
   - 人机对战（PVE）：创建 PVE 房间，与 AI 对战
   - 创建房间（PVP）：创建 PVP 房间，邀请好友加入
   - 在线匹配：快速匹配其他玩家（UI 存在，但功能暂未开放，显示"匹配功能暂未开放"提示）
   - 加入房间：通过房间 ID 加入其他玩家的房间
3. **GameRoomPage**：游戏房间，核心页面
4. **ProfilePage**：个人中心，用户信息管理（包含 `ProfileHome` 和 `ProfileEdit` 组件）
5. **MessageCenterPage**：消息中心，查看和管理所有通知
6. **SessionMonitorPage**：会话监控（公开页面，无需登录，每 2 秒轮询一次）
7. **NotFoundPage**：404 页面

### 路由保护

`ProtectedRoute` 组件检查 `isAuthenticated`，未登录则重定向到首页。

---

## 认证与授权

### 认证流程

1. **应用启动**：`AuthProvider` 初始化，检查 Token
2. **Token 验证**：通过 `/game-service/me` 验证 Token 有效性
3. **Token 获取**：无效则从 Gateway `/token` 获取新 Token
4. **登录跳转**：仍无效则跳转 Keycloak 登录页
5. **登录回调**：Keycloak 回调后，Gateway 设置 Cookie，前端获取 Token

### Token 管理

- **存储**：`localStorage`（key: `access_token`）
- **获取优先级**：
  1. Gateway `/token` 接口（支持自动刷新）
  2. 本地存储的 Token
- **验证**：每次使用前通过 `/game-service/me` 验证
- **刷新**：Gateway 自动刷新（基于 Cookie），前端无需手动处理

### 401 处理

检测到 401 响应时：

1. **显示登录失效弹窗**：引导用户重新登录
2. **清理本地 Token**：`clearToken()`
3. **设置登出标志**：`sessionLoggingOut = true`
4. **WebSocket 断开**：检测到 401 时，设置 `isManualDisconnect = true`，不触发自动重连

---

## 错误处理

### HTTP 错误处理

**统一处理**（apiClient.js）：
- 401：触发登出流程
- 其他错误：解析响应，提取 `message` 字段，抛出 Error

**组件级处理**：
- 使用 `try-catch` 捕获错误
- 显示用户友好的错误提示
- 静默处理非关键错误（如用户信息加载失败）

### WebSocket 错误处理

**连接错误**：
- `socket.onerror`：SockJS 错误，触发重连
- `stomp.onStompError`：STOMP 错误，401 则登出，其他则重连
- 连接超时：10 秒未连接成功，触发重连

**消息错误**：
- JSON 解析失败：静默忽略
- 订阅回调错误：捕获异常，不影响其他订阅

### 用户提示

- **Toast 消息**：临时提示（3 秒自动消失）
- **弹窗**：重要操作确认（如被踢出房间）
- **状态指示器**：连接状态（绿色圆点=已连接，红色圆点=断开，旋转图标=重连中）

---

## 性能优化

### 1. 代码分割

- 使用 React Router 的懒加载（未来可优化）
- 组件按需加载

### 2. 状态优化

- **useCallback**：稳定回调函数引用，避免不必要的重渲染
- **useMemo**：缓存计算结果
- **ref**：存储不需要触发重渲染的值（如 `seatKeyRef`、`boardRef`）

### 3. 请求优化

- **用户信息缓存**：避免重复请求同一用户信息
- **批量获取**：`getUserInfos()` 批量获取用户信息
- **懒加载**：聊天消息中缺失用户信息时才请求

### 4. WebSocket 优化

- **订阅管理**：避免重复订阅同一 topic
- **连接复用**：Chat WS 全局连接，多个组件共享
- **重连优化**：指数退避，避免频繁重连

### 5. 渲染优化

- **条件渲染**：使用 `&&` 和三元运算符
- **列表 key**：使用稳定的 key（如 `crypto.randomUUID()`）
- **避免不必要的状态更新**：使用 `useRef` 存储不需要触发渲染的值

---

---

## 十二、WebSocket 连接机制详解

### 12.1 Game WebSocket（gomokuSocket.js）

#### 12.1.1 连接建立流程

**完整代码实现**：

```javascript
// gomokuSocket.js
export async function connectWebSocket(callbacks = {}) {
  currentCallbacks = callbacks
  isManualDisconnect = false
  reconnectAttempts = 0
  clearTimeout(reconnectTimer)
  reconnectTimer = null
  
  const wasConnected = stomp?.connected || false
  await connectWebSocketInternal(callbacks, !wasConnected)
}

async function connectWebSocketInternal(callbacks = {}, isInitialConnect = false) {
  if (typeof window === 'undefined') return

  // 如果已连接，直接回调
  if (stomp && stomp.connected) {
    reconnectAttempts = 0
    clearTimeout(reconnectTimer)
    reconnectTimer = null
    callbacks.onConnect?.()
    return
  }

  // 关闭旧连接
  if (socket) {
    try {
      socket.close()
    } catch (e) {
      // ignore
    }
  }

  // 获取 Token
  const token = await ensureAuthenticated()
  if (!token) {
    scheduleReconnect(isInitialConnect)
    return
  }

  // 创建 SockJS 连接
  const wsUrl = `/game-service/ws?access_token=${encodeURIComponent(token)}`
  socket = new SockJS(wsUrl)
  
  // Socket 事件处理
  socket.onclose = (event) => {
    callbacks.onDisconnect?.()
    if (!isManualDisconnect) {
      scheduleReconnect(false)  // 此时不是初始连接
    }
  }
  
  socket.onerror = (error) => {
    if (!isManualDisconnect && socket.readyState === SockJS.CLOSED) {
      callbacks.onDisconnect?.()
      scheduleReconnect(false)  // 此时不是初始连接
    }
  }

  // 创建 STOMP 客户端
  stomp = Stomp.over(socket)
  
  // 配置心跳：客户端每 5 秒发送一次心跳，期望服务端每 5 秒发送一次心跳
  stomp.heartbeat.outgoing = 5000
  stomp.heartbeat.incoming = 5000
  
  // 清理旧的定时器
  if (heartbeatCheckInterval) {
    clearInterval(heartbeatCheckInterval)
    heartbeatCheckInterval = null
  }

  const headers = { Authorization: 'Bearer ' + token }
  
  // 连接超时检测（10 秒）
  const connectTimeout = setTimeout(() => {
    if (!stomp.connected) {
      callbacks.onError?.(new Error('连接超时'))
      if (!isManualDisconnect) {
        scheduleReconnect(isInitialConnect)
      }
    }
  }, 10000)

  try {
    stomp.connect(headers, (frame) => {
      clearTimeout(connectTimeout)
      reconnectAttempts = 0
      clearTimeout(reconnectTimer)
      reconnectTimer = null
      
      // 连接成功后，启动心跳检测定时器
      if (heartbeatCheckInterval) {
        clearInterval(heartbeatCheckInterval)
      }
      heartbeatCheckInterval = setInterval(() => {
        if (isManualDisconnect) {
          clearInterval(heartbeatCheckInterval)
          heartbeatCheckInterval = null
          return
        }
        
        // 检测连接状态
        if (stomp && !stomp.connected) {
          clearInterval(heartbeatCheckInterval)
          heartbeatCheckInterval = null
          callbacks.onDisconnect?.()
          scheduleReconnect(false)  // 定期检查发现的断开，不是初始连接
        }
      }, 5000)  // 每 5 秒检查一次
      
      // 订阅系统踢线通知
      try {
        subscribeSystemKick((payload) => {
          const reason = payload?.reason || '账号已在其他终端登录'
          isManualDisconnect = true
          callbacks.onKicked?.(reason)
          disconnectWebSocket()
        })
      } catch {
        // ignore
      }

      callbacks.onConnect?.()
    }, (error) => {
      clearTimeout(connectTimeout)
      if (isUnauthorizedWebSocketError(error)) {
        isManualDisconnect = true
        performSessionLogout('WebSocket 会话已失效，请重新登录')
        return
      }
      callbacks.onError?.(error)
      if (!isManualDisconnect) {
        scheduleReconnect(isInitialConnect)
      }
    })
  } catch (error) {
    clearTimeout(connectTimeout)
    callbacks.onError?.(error)
    if (!isManualDisconnect) {
      scheduleReconnect(isInitialConnect)
    }
  }
}
```

#### 12.1.2 心跳机制详解

**配置**：
- **客户端发送心跳**：`stomp.heartbeat.outgoing = 5000`（每 5 秒）
- **期望服务端心跳**：`stomp.heartbeat.incoming = 5000`（每 5 秒）
- **主动检测**：连接成功后，每 5 秒检查一次 `stomp.connected` 状态

**实现细节**：
1. STOMP 客户端自动发送心跳（PING）
2. 服务端自动回复心跳（PONG）
3. 前端额外启动定时器，每 5 秒检查 `stomp.connected`，如果为 `false`，触发重连

#### 12.1.3 自动重连机制详解

**重连策略**（指数退避）：
```javascript
function getReconnectDelay(attempt) {
  return Math.min(INITIAL_RECONNECT_DELAY * Math.pow(2, attempt), MAX_RECONNECT_DELAY)
}
// INITIAL_RECONNECT_DELAY = 1000 (1秒)
// MAX_RECONNECT_DELAY = 30000 (30秒)
// MAX_RECONNECT_ATTEMPTS = 10
```

**重连延迟序列**：1s → 2s → 4s → 8s → 16s → 30s（上限）→ 30s → ...

**重连触发条件**：
1. `socket.onclose` 事件
2. `socket.onerror` 事件（且 `socket.readyState === SockJS.CLOSED`）
3. `stomp.onStompError` 回调（非 401 错误）
4. 连接超时（10 秒内未连接成功）
5. 心跳检测失败（定期检查发现 `stomp.connected === false`）

**不重连场景**：
1. 手动断开（`isManualDisconnect = true`）
2. 401 未授权错误（直接触发登出）
3. 收到系统踢线通知（单设备登录/登出）

#### 12.1.4 断开机制

**手动断开**：
```javascript
export function disconnectWebSocket() {
  isManualDisconnect = true
  clearTimeout(reconnectTimer)
  reconnectTimer = null
  reconnectAttempts = 0
  currentCallbacks = null
  
  // 清理心跳检测定时器
  if (heartbeatCheckInterval) {
    clearInterval(heartbeatCheckInterval)
    heartbeatCheckInterval = null
  }

  // 清理所有订阅
  subscriptions.forEach((sub) => sub.unsubscribe())
  subscriptions.clear()

  // 断开 STOMP 和 Socket
  if (stomp) {
    stomp.disconnect()
    stomp = null
  }
  
  if (socket) {
    socket.close()
    socket = null
  }
}

export function isConnected() {
  return Boolean(stomp?.connected)
}
```

### 12.2 Chat WebSocket（chatSocket.js）

#### 12.2.1 连接建立流程

**完整代码实现**：

```javascript
// chatSocket.js
export async function connectChatWebSocket(callbacks = {}) {
  // 添加回调监听器（支持多个监听器）
  if (callbacks && Object.keys(callbacks).length > 0) {
    callbackListeners.add(callbacks)
  }
  
  // 如果已经连接，直接通知新添加的监听器
  if (stomp && stomp.connected) {
    if (callbacks && typeof callbacks.onConnect === 'function') {
      try {
        callbacks.onConnect()
      } catch (error) {
        // 忽略错误
      }
    }
    return
  }
  
  // 如果正在重连，只添加监听器，不干扰重连过程
  if (reconnectTimer) {
    return
  }
  
  // 初始化连接
  isManualDisconnect = false
  reconnectAttempts = 0
  await connectChatWebSocketInternal(true)
}

async function connectChatWebSocketInternal(isInitialConnect = false) {
  if (typeof window === 'undefined') return
  
  // 如果已连接，直接通知所有监听器
  if (stomp && stomp.connected) {
    reconnectAttempts = 0
    clearTimeout(reconnectTimer)
    reconnectTimer = null
    notifyListeners('onConnect')
    return
  }
  
  // 关闭旧连接
  if (socket) {
    try {
      socket.close()
    } catch (e) {
      // ignore
    }
  }

  const token = await ensureAuthenticated()
  if (!token) {
    scheduleReconnect(isInitialConnect)
    return
  }

  const wsUrl = `/chat-service/ws?access_token=${encodeURIComponent(token)}`
  socket = new SockJS(wsUrl)
  
  socket.onclose = (event) => {
    notifyListeners('onDisconnect')
    if (!isManualDisconnect) {
      scheduleReconnect(false)  // 此时不是初始连接
    }
  }
  
  socket.onerror = (error) => {
    if (!isManualDisconnect && socket.readyState === SockJS.CLOSED) {
      notifyListeners('onDisconnect')
      scheduleReconnect(false)  // 此时不是初始连接
    }
  }

  stomp = Stomp.over(socket)
  
  // 配置心跳：客户端每 5 秒发送一次心跳，期望服务端每 5 秒发送一次心跳
  stomp.heartbeat.outgoing = 5000
  stomp.heartbeat.incoming = 5000
  
  // 清理旧的定时器
  if (heartbeatCheckInterval) {
    clearInterval(heartbeatCheckInterval)
    heartbeatCheckInterval = null
  }

  const headers = { Authorization: 'Bearer ' + token }
  const connectTimeout = setTimeout(() => {
    if (!stomp || !stomp.connected) {
      notifyListeners('onError', new Error('连接超时'))
      if (!isManualDisconnect) {
        scheduleReconnect(isInitialConnect)
      }
    }
  }, 10000)

  try {
    stomp.connect(headers, (frame) => {
      clearTimeout(connectTimeout)
      reconnectAttempts = 0
      clearTimeout(reconnectTimer)
      reconnectTimer = null
      
      // 连接成功后，启动心跳检测定时器
      if (heartbeatCheckInterval) {
        clearInterval(heartbeatCheckInterval)
      }
      heartbeatCheckInterval = setInterval(() => {
        if (isManualDisconnect) {
          clearInterval(heartbeatCheckInterval)
          heartbeatCheckInterval = null
          return
        }
        
        if (stomp && !stomp.connected) {
          clearInterval(heartbeatCheckInterval)
          heartbeatCheckInterval = null
          notifyListeners('onDisconnect')
          scheduleReconnect(false)  // 定期检查发现的断开，不是初始连接
        }
      }, 5000)  // 每 5 秒检查一次
      
      // 订阅系统踢线通知
      try {
        subscribeSystemKick((payload) => {
          const reason = payload?.reason || '账号已在其他终端登录'
          isManualDisconnect = true
          notifyListeners('onKicked', reason)
          disconnectChatWebSocket()
        })
      } catch {
        // ignore
      }

      // 订阅业务通知（好友申请等）
      try {
        subscribeUserNotify((payload) => {
          notifyListeners('onNotify', payload)
          // 同时广播浏览器事件（兜底机制）
          if (typeof window !== 'undefined') {
            window.__ghNotifyBuffer = window.__ghNotifyBuffer || []
            window.__ghNotifyBuffer.push(payload)
            const evt = new CustomEvent('gh-notify', { detail: payload })
            window.dispatchEvent(evt)
          }
        })
      } catch (err) {
        // ignore
      }

      notifyListeners('onConnect')
    }, (error) => {
      clearTimeout(connectTimeout)
      if (isUnauthorized(error)) {
        isManualDisconnect = true
        performSessionLogout('聊天会话已失效，请重新登录')
        return
      }
      notifyListeners('onError', error)
      if (!isManualDisconnect) {
        scheduleReconnect(isInitialConnect)
      }
    })
  } catch (error) {
    clearTimeout(connectTimeout)
    notifyListeners('onError', error)
    if (!isManualDisconnect) {
      scheduleReconnect(isInitialConnect)
    }
  }
}
```

#### 12.2.2 多监听器支持

Chat WebSocket 使用 `Set` 存储多个回调对象，支持多个组件同时监听同一连接：

```javascript
const callbackListeners = new Set()

function notifyListeners(method, ...args) {
  callbackListeners.forEach((callbacks) => {
    if (callbacks && typeof callbacks[method] === 'function') {
      try {
        callbacks[method](...args)
      } catch (error) {
        // 监听器回调出错，不影响其他监听器
      }
  })
}
```

**使用场景**：
- `useGlobalChatWs`（全局布局）：监听连接状态，接收通知
- `useChatRoomWs`（房间页面）：监听连接状态，接收房间消息
- `GlobalChat`（私聊组件）：监听连接状态，接收私聊消息

#### 12.2.3 心跳和重连机制

与 Game WebSocket 相同，详见 12.1.2 和 12.1.3。

---

## 十三、好友申请机制

### 13.1 申请加好友

**API 调用**（`friendApi.js`）：
```javascript
export async function applyFriend(targetUserId, requestMessage) {
  const requestData = { 
    targetUserId,
    requestMessage: requestMessage || null
  }
  
  const response = await post('/system-service/api/friends/apply', requestData)
  
  // 检查是否自动通过
  const message = response.message || ''
  const autoAccepted = message.includes('已自动成为好友') || message.includes('自动成为好友')
  
  return {
    autoAccepted,
    message: response.message || '申请已发送'
  }
}
```

**使用场景**（`GameRoomPage.jsx`）：
- 在玩家信息弹窗中，点击"加好友"按钮
- 调用 `applyFriend(targetUserId, requestMessage)`
- 如果自动通过，显示"已自动成为好友"提示

### 13.2 同意/拒绝好友申请

**API 调用**（`friendApi.js`）：
```javascript
export async function acceptFriendRequest(requestId) {
  return postFriendActionWithFallback(requestId, 'accept', '同意好友申请失败')
}

export async function rejectFriendRequest(requestId) {
  return postFriendActionWithFallback(requestId, 'reject', '拒绝好友申请失败')
}
```

**使用场景**：
- **Header 通知铃铛**：点击通知中的"同意"/"拒绝"按钮
- **MessageCenterPage**：在消息中心页面处理好友申请

**处理流程**：
1. 调用 API 同意/拒绝申请
2. 更新通知状态（标记为已读，移除操作按钮）
3. 如果同意，触发 `gh-friend-list-refresh` 事件，通知 `GlobalChat` 刷新好友列表

### 13.3 好友列表自动刷新机制

**监听好友申请通过通知**（`GlobalChat.jsx`）：

```javascript
// 监听 FRIEND_RESULT 通知（申请人收到）
useEffect(() => {
  const handleFriendResultNotification = (event) => {
    const notify = event?.detail || {}
    
    // 只处理 FRIEND_RESULT 类型的通知
    if (notify.type !== 'FRIEND_RESULT') return
    
    // 检查是否是通过（accepted=true）
    const payload = notify.payload || {}
    const isAccepted = payload.accepted === true || payload.result === 'ACCEPTED'
    
    if (isAccepted) {
      // 延迟一下，确保后端数据已更新
      setTimeout(() => {
        loadFriendsList()
      }, 500)
    }
  }

  // 监听好友列表刷新事件（同意方主动触发）
  const handleFriendListRefresh = (event) => {
    const detail = event?.detail || {}
    if (detail.action === 'ACCEPT') {
      setTimeout(() => {
        loadFriendsList()
      }, 500)
    }
  }

  window.addEventListener('gh-notify', handleFriendResultNotification)
  window.addEventListener('gh-friend-list-refresh', handleFriendListRefresh)
  
  return () => {
    window.removeEventListener('gh-notify', handleFriendResultNotification)
    window.removeEventListener('gh-friend-list-refresh', handleFriendListRefresh)
  }
}, [loadFriendsList])
```

**触发时机**：
1. **申请人收到 FRIEND_RESULT 通知**：通过 WebSocket 接收，自动刷新好友列表
2. **同意方主动触发**：在 `Header.jsx` 或 `MessageCenterPage.jsx` 中同意申请后，触发 `gh-friend-list-refresh` 事件

---

## 十四、消息通知机制

### 14.1 WebSocket 通知订阅

**订阅路径**：`/user/queue/notify`（点对点，Spring 自动路由到对应用户）

**通知类型**：
- `FRIEND_REQUEST`：好友申请通知
- `FRIEND_RESULT`：好友申请结果通知（申请人收到）
- `SYSTEM`：系统通知

**通知格式**：
```javascript
{
  id: string,              // 通知ID
  type: string,            // 通知类型
  title: string,           // 标题
  content: string,         // 内容
  status: 'UNREAD' | 'READ',
  createdAt: number,       // 时间戳
  actions: string[],       // 可用操作 ['ACCEPT', 'REJECT']
  payload: {
    friendRequestId: string,
    notificationId: string,
    requestMessage: string,
    accepted: boolean,     // FRIEND_RESULT 专用
    result: string         // FRIEND_RESULT 专用
  }
}
```

### 14.2 通知分发机制

**多级分发**（`chatSocket.js`）：

```javascript
subscribeUserNotify((payload) => {
  // 1. 通知所有 WebSocket 监听器
  notifyListeners('onNotify', payload)
  
  // 2. 添加到缓冲区（兜底机制）
  window.__ghNotifyBuffer = window.__ghNotifyBuffer || []
  window.__ghNotifyBuffer.push(payload)
  
  // 3. 广播浏览器事件（供 Header 等组件使用）
  const evt = new CustomEvent('gh-notify', { detail: payload })
  window.dispatchEvent(evt)
})
```

**接收组件**：
1. **Header.jsx**：监听 `gh-notify` 事件，更新通知列表和未读计数
2. **MessageCenterPage.jsx**：监听 `gh-notify` 事件，实时添加新通知
3. **GlobalChat.jsx**：监听 `gh-notify` 事件，处理好友申请结果通知

### 14.3 未读计数管理

**获取未读数**（`notificationApi.js`）：
```javascript
export async function fetchUnreadCount() {
  return get('/system-service/api/notifications/unread-count')
}
```

**更新未读数**：
- **初始加载**：`Header.jsx` 在组件挂载时调用 `fetchUnreadCount()`
- **收到新通知**：`Header.jsx` 监听 `gh-notify` 事件，未读计数 +1
- **标记已读**：点击通知或调用 `markNotificationRead(id)`，未读计数 -1
- **全部已读**：调用 `markAllNotificationsRead()`，未读计数清零

---

## 十五、私聊机制

### 15.1 私聊会话管理

**会话列表加载**（`GlobalChat.jsx`）：

```javascript
useEffect(() => {
  if (!isAuthenticated) return
  
  ;(async () => {
    try {
      const sessionsResponse = await get('/chat-service/api/sessions')
      if (sessionsResponse && Array.isArray(sessionsResponse)) {
        // 将会话列表转换为 threads
        sessionsResponse.forEach(session => {
          if (session.sessionType === 'PRIVATE' && session.otherUserId) {
            const threadId = `friend_${session.otherUserId}`
            
            newThreads.push({
              id: threadId,
              title: session.otherUserNickname || '用户',
              avatar: session.otherUserAvatarUrl || DEFAULT_AVATAR,
              lastMessage: session.lastMessage || null,
              lastTime: session.lastMessageTime ? new Date(session.lastMessageTime) : null,
              unread: session.unreadCount || 0,  // 后端返回的未读数
            })
          }
        })
      }
    } catch (error) {
      // 静默失败
    }
  })()
}, [isAuthenticated])
```

**会话信息**：
- `sessionId`：会话ID（用于标记已读）
- `otherUserId`：对方用户ID
- `otherUserNickname`：对方昵称
- `otherUserAvatarUrl`：对方头像
- `lastMessage`：最后一条消息
- `lastMessageTime`：最后消息时间
- `unreadCount`：未读消息数

### 15.2 私聊消息收发

**订阅私聊消息**（`chatSocket.js`）：

```javascript
export function subscribePrivateChat(onMessage) {
  const client = getClient()
  const topic = '/user/queue/chat.private'  // 点对点，Spring 自动路由
  
  const sub = client.subscribe(topic, (frame) => {
    try {
      const payload = JSON.parse(frame.body)
      onMessage(payload)
    } catch (e) {
      // 解析失败，静默处理
    }
  })
  
  subscriptions.set(topic, sub)
  return () => sub.unsubscribe()
}
```

**发送私聊消息**（`chatSocket.js`）：

```javascript
export function sendPrivateChat(targetUserId, content, clientOpId) {
  const client = getClient()
  client.publish({
    destination: '/app/chat.private.send',
    body: JSON.stringify({
      targetUserId,
      content,
      clientOpId: clientOpId || crypto?.randomUUID?.() || String(Date.now()),
    }),
  })
}
```

**消息格式**：
```javascript
{
  type: 'PRIVATE',
  senderId: string,
  senderName: string,      // 可选
  targetUserId: string,
  content: string,
  timestamp: number,
  clientOpId: string       // 用于去重
}
```

### 15.3 已读未读机制

**标记已读**（`GlobalChat.jsx`）：

```javascript
const markSessionAsRead = useCallback(async (threadId, friendId) => {
  // 获取 sessionId
  let sessionId = threadIdToSessionId[threadId]
  
  if (!sessionId) {
    // 通过 friendId 查询私聊会话的 sessionId
    try {
      const response = await get(`/chat-service/api/sessions/private/${friendId}`)
      if (response && response.sessionId) {
        sessionId = response.sessionId
        setThreadIdToSessionId(prev => ({ ...prev, [threadId]: sessionId }))
      }
    } catch (error) {
      // 如果返回 404，说明会话不存在（还没有发送过消息），这是正常的
      if (error?.response?.status === 404) {
        return
      }
    }
  }
  
  // 调用标记已读API
  if (sessionId) {
    try {
      await post(`/chat-service/api/sessions/${sessionId}/read`)
    } catch (error) {
      // 静默失败
    }
  }
}, [threadIdToSessionId])
```

**触发时机**：
1. **打开会话**：点击会话列表中的会话，自动标记已读
2. **收到新消息**：如果当前会话是活动会话，自动标记已读
3. **查看消息**：在会话窗口中查看消息时，自动标记已读

**未读计数**：
- **后端返回**：会话列表 API 返回 `unreadCount`
- **前端更新**：收到新消息时，如果当前会话不是活动会话，未读数 +1
- **标记已读后**：调用标记已读 API 后，前端立即将未读数清零

### 15.4 历史消息加载

**加载历史**（`GlobalChat.jsx`）：

```javascript
const handleOpenThread = useCallback(async (threadId) => {
  setActiveThreadId(threadId)
  
  // 如果是私聊会话，加载历史消息
  if (threadId.startsWith('friend_')) {
    const friendId = threadId.replace('friend_', '')
    
    // 如果还没有加载过历史消息
    if (!historyLoadedThreads.has(threadId)) {
      try {
        const response = await get(`/chat-service/api/private/${friendId}/history?limit=100`)
        if (response && Array.isArray(response)) {
          // 将历史消息添加到会话中
          response.forEach((msg) => {
            const isSelf = currentUserId && (msg.senderId === currentUserId || String(msg.senderId) === String(currentUserId))
            const messageId = msg.clientOpId || `${msg.timestamp}_${msg.senderId}`
            addMessage({
              text: msg.content || '',
              type: isSelf ? 'self' : 'other',
              timestamp: msg.timestamp ? new Date(msg.timestamp) : new Date(),
              threadId,
              isHistory: true,  // 标记为历史消息，不增加未读数
              messageId: messageId,  // 用于去重
            })
          })
          // 标记为已加载
          setHistoryLoadedThreads((prev) => new Set([...prev, threadId]))
        }
      } catch (error) {
        // 静默失败
      }
    }
    
    // 标记消息为已读
    await markSessionAsRead(threadId, friendId)
  }
}, [historyLoadedThreads, currentUserId, addMessage, markSessionAsRead])
```

**去重机制**：
- 使用 `clientOpId` 或 `timestamp + senderId` 作为 `messageId`
- 添加消息前检查是否已存在相同 `messageId` 的消息
- 历史消息和实时消息使用相同的去重逻辑

---

## 十六、房间内会话机制

### 16.1 房间聊天历史加载

**加载历史**（`GameRoomPage.jsx`）：

```javascript
useEffect(() => {
  if (!roomId) return
  
  let cancelled = false
  
  ;(async () => {
    try {
      // 先拉取房间聊天历史（最近 50 条）
      const history = await getRoomChatHistory(roomId, 50)
      if (!cancelled && Array.isArray(history)) {
        const mapped = history.map((msg) => 
          mapHistoryMessage(msg, currentUserId, resolveDisplayName)
        ).filter(Boolean)
        setChatHistory(mapped)
      }
    } catch (error) {
      // 静默失败
    }
  })()
  
  return () => {
    cancelled = true
  }
}, [roomId, currentUserId, resolveDisplayName])
```

### 16.2 房间聊天消息订阅

**订阅房间消息**（`useChatRoomWs.js`）：

```javascript
export function useChatRoomWs({ roomId, onMessage }) {
  useEffect(() => {
    if (!roomId) return undefined
    
    const callbacks = {
      onConnect: () => {
        // 连接成功后，订阅房间聊天
        unsubRef.current = subscribeRoomChat(roomId, (evt) => {
          onMessage?.(evt)
        })
      },
      // ...
    }
    
    connectChatWebSocket(callbacks)
    
    return () => {
      unsubRef.current?.()
      removeChatWebSocketCallbacks(callbacks)
    }
  }, [roomId, onMessage])
}
```

**消息格式**：
```javascript
{
  type: 'ROOM',
  senderId: string,
  senderName: string,      // 可选
  roomId: string,
  content: string,
  timestamp: number,
  clientOpId: string       // 用于去重
}
```

---

## 十七、前后端交互 API 清单

### 17.1 HTTP API

#### 17.1.1 游戏相关 API（gameApi.js）

| 方法 | 路径 | 说明 | 请求参数 | 响应 |
|------|------|------|----------|------|
| POST | `/game-service/api/gomoku/new` | 创建房间 | `mode`, `aiPiece`, `rule` | `{ code: 200, data: "房间ID" }` |
| POST | `/game-service/api/gomoku/rooms/{roomId}/join` | 加入房间 | - | `{ code: 200, data: { side: 'X' \| 'O' } }` |
| POST | `/game-service/api/gomoku/rooms/{roomId}/leave` | 离开房间 | - | `{ code: 200 }` |
| GET | `/game-service/api/gomoku/rooms/{roomId}/view` | 获取房间快照 | - | `{ code: 200, data: GomokuSnapshot }` |
| GET | `/game-service/api/gomoku/rooms` | 获取房间列表 | `cursor`, `limit` | `{ code: 200, data: { items, nextCursor, hasMore } }` |
| GET | `/game-service/api/ongoing-game` | 获取进行中的对局 | - | `{ code: 200, data: { hasOngoing, roomId, title } }` |
| POST | `/game-service/api/ongoing-game/end` | 结束对局 | `{ roomId }` | `{ code: 200 }` |
| GET | `/game-service/me` | 获取当前用户信息（gameApi.js 封装，用于验证 Token） | - | `{ code: 200, data: UserInfo }` |
| POST | `/system-service/api/users/users/batch` | 批量获取用户信息 | `string[]` (userIds) | `{ code: 200, data: UserInfo[] }` |
| GET | `/system-service/api/users/users/{userId}` | 获取单个用户信息 | - | `{ code: 200, data: UserInfo }` |

**注意**：
- `getMe()` 函数在 `gameApi.js` 中定义，但实际获取用户信息主要使用 `authService.js` 的 `getUserInfo()`（优先 system-service）
- `getUserInfo()` 调用顺序：`POST /system-service/api/users/sync` → `GET /system-service/api/users/me/profile` → `GET /system-service/api/users/me` → `GET /game-service/me`

**兼容性函数**（已废弃，使用上述函数替代）：
- `getPlayerInfos(userIds)`：已废弃，使用 `getUserInfos(userIds)` 替代
- `getPlayerInfo(userId)`：已废弃，使用 `getUserInfo(userId)` 替代

#### 17.1.2 聊天相关 API

**chatApi.js 封装**：
| 方法 | 路径 | 说明 | 请求参数 | 响应 |
|------|------|------|----------|------|
| GET | `/chat-service/api/rooms/{roomId}/history` | 获取房间聊天历史 | `limit` | `ChatMessage[]` |

**直接使用 apiClient 的 API**（在 `GlobalChat.jsx` 中直接调用）：
| 方法 | 路径 | 说明 | 请求参数 | 响应 |
|------|------|------|----------|------|
| GET | `/chat-service/api/private/{friendId}/history` | 获取私聊历史 | `limit` | `ChatMessage[]` |
| GET | `/chat-service/api/sessions` | 获取会话列表 | - | `ChatSession[]` |
| GET | `/chat-service/api/sessions/private/{friendId}` | 获取私聊会话信息（仅查询，不创建） | - | `{ sessionId, ... }` 或 404（会话不存在） |
| POST | `/chat-service/api/sessions/{sessionId}/read` | 标记会话已读 | - | `{ code: 200 }` |

**注意**：私聊相关的 API 调用主要在 `GlobalChat.jsx` 中直接使用 `apiClient` 的 `get` 和 `post` 函数，未单独封装到 `chatApi.js`。

#### 17.1.3 好友相关 API（friendApi.js）

| 方法 | 路径 | 说明 | 请求参数 | 响应 |
|------|------|------|----------|------|
| POST | `/system-service/api/friends/apply` | 申请加好友 | `{ targetUserId, requestMessage }` | `{ code: 200, message: string }` |
| POST | `/system-service/api/friends/requests/{requestId}/accept` | 同意好友申请 | - | `{ code: 200 }` |
| POST | `/system-service/api/friends/requests/{requestId}/reject` | 拒绝好友申请 | - | `{ code: 200 }` |
| GET | `/system-service/api/friends` | 获取好友列表 | - | `{ code: 200, data: Friend[] }` |

#### 17.1.4 通知相关 API（notificationApi.js）

| 方法 | 路径 | 说明 | 请求参数 | 响应 |
|------|------|------|----------|------|
| GET | `/system-service/api/notifications` | 获取通知列表 | `status`, `limit` | `{ code: 200, data: Notification[] }` |
| GET | `/system-service/api/notifications/unread-count` | 获取未读计数 | - | `{ code: 200, data: number }` |
| POST | `/system-service/api/notifications/{id}/read` | 标记通知已读 | - | `{ code: 200 }` |
| POST | `/system-service/api/notifications/read-all` | 标记全部已读 | - | `{ code: 200 }` |

#### 17.1.5 用户相关 API（userApi.js）

| 方法 | 路径 | 说明 | 请求参数 | 响应 |
|------|------|------|----------|------|
| GET | `/system-service/api/users/me/profile` | 获取用户资料（ProfilePage 使用） | - | `{ code: 200, data: UserProfile }` |
| PUT | `/system-service/api/users/me/profile` | 更新用户资料 | `{ nickname, email, phone, avatarUrl, bio, locale, timezone, settings }` | `{ code: 200, data: UserProfile }` |
| POST | `/system-service/api/files/upload/avatar/temp` | 上传头像（临时） | `FormData` (file) | `{ code: 200, data: string }` (临时文件 URL) |

**注意**：
- `getCurrentUserProfile()` 用于 ProfilePage 获取完整用户资料（直接调用 `/system-service/api/users/me/profile`）
- `authService.js` 的 `getUserInfo()` 也调用 `/system-service/api/users/me/profile`，但会先调用 `POST /system-service/api/users/sync` 同步用户（创建/首次登录场景，避免先查 404）

#### 17.1.6 认证相关 API（authService.js）

| 方法 | 路径 | 说明 | 请求参数 | 响应 |
|------|------|------|----------|------|
| GET | `/token` | 获取 Token（Gateway） | - | `{ access_token: string }` |
| POST | `/system-service/api/users/sync` | 同步用户信息（创建/首次登录场景，避免先查 404） | - | `{ code: 200, data: UserInfo }` |
| GET | `/system-service/api/users/me/profile` | 获取用户完整资料（优先） | - | `{ code: 200, data: UserProfile }` |
| GET | `/system-service/api/users/me` | 获取用户基础信息（降级） | - | `{ code: 200, data: UserInfo }` |
| GET | `/game-service/me` | 获取用户信息（兜底，也用于验证 Token） | - | `{ code: 200, data: UserInfo }` |
| GET | `/logout` | 登出（Gateway） | - | 重定向到登录页 |

**主要函数**：
- `ensureAuthenticated(autoLogin)`：确保已认证（优先 Gateway `/token`，失败则使用本地存储）
- `getTokenFromGateway()`：从 Gateway 获取 Token（`GET /token`）
- `validateToken(token)`：验证 Token 有效性（调用 `GET /game-service/me`）
- `performSessionLogout(reason)`：执行登出流程（显示提示，清理 Token，断开 WebSocket）
- `handleAuthExpiredResponse(res, context)`：处理 401 响应，显示登录失效弹窗
- `getUserInfo()`：获取用户信息（调用顺序：`POST /system-service/api/users/sync` → `GET /system-service/api/users/me/profile` → `GET /system-service/api/users/me` → `GET /game-service/me`）
- `initAndLogin()`：初始化并触发登录流程
- `logoutFromGateway(redirectUri)`：从 Gateway 登出（`GET /logout`）

#### 17.1.7 会话监控 API（sessionMonitor.js）

| 方法 | 路径 | 说明 | 请求参数 | 响应 |
|------|------|------|----------|------|
| GET | `http://localhost:8082/internal/sessions` | 获取所有在线用户会话信息（直连 system-service，绕过网关） | - | `SessionSnapshot[]` |

**注意**：此 API 直接请求 system-service，不经过 Gateway，用于会话监控页面（`SessionMonitorPage`）。

### 17.2 WebSocket API

#### 17.2.1 Game WebSocket（game-service）

**连接地址**：`/game-service/ws?access_token={token}`

**订阅（接收）**：

| 路径 | 说明 | 消息格式 |
|------|------|----------|
| `/topic/room.{roomId}` | 房间事件（广播） | `{ type: 'STATE' \| 'SNAPSHOT' \| 'TICK' \| 'ERROR', ... }` |
| `/user/queue/gomoku.seat` | 座位分配（点对点） | `{ seatKey: string, side: 'X' \| 'O' }` |
| `/user/queue/gomoku.full` | 完整同步（点对点） | `GomokuSnapshot` |
| `/user/queue/gomoku.kicked` | 被踢事件（点对点，房间内被房主踢出） | `{ reason: string }` |
| `/user/queue/system.kick` | 系统踢线通知（点对点，单设备登录/登出等） | `{ reason: string }` |

**发送**：

| 路径 | 说明 | 请求体 |
|------|------|--------|
| `/app/gomoku.resume` | 恢复连接 | `{ roomId, seatKey }` |
| `/app/gomoku.place` | 落子 | `{ roomId, x, y, side, seatKey }` |
| `/app/gomoku.ready` | 准备/取消准备 | `{ roomId, seatKey }` |
| `/app/gomoku.start` | 开始游戏 | `{ roomId, seatKey }` |
| `/app/gomoku.resign` | 认输 | `{ roomId, seatKey }` |
| `/app/gomoku.restart` | 重新开始 | `{ roomId, seatKey }` |
| `/app/gomoku.kick` | 踢出玩家 | `{ roomId, targetUserId, seatKey }` |

**工具函数**：
- `isConnected()`：检查 WebSocket 是否已连接（返回 `Boolean(stomp?.connected)`）
- `subscribeRoom(roomId, onEvent)`：订阅房间事件（`/topic/room.{roomId}`）
- `subscribeSeatKey(onSeatKey)`：订阅座位分配（`/user/queue/gomoku.seat`）
- `subscribeFullSync(onFullSync)`：订阅完整同步（`/user/queue/gomoku.full`）
- `subscribeKicked(onKicked)`：订阅房间内被踢事件（`/user/queue/gomoku.kicked`），返回取消订阅函数
- `sendResume(roomId, seatKey)`：发送恢复连接请求
- `sendPlace(roomId, x, y, side, seatKey)`：发送落子请求
- `sendReady(roomId, seatKey)`：发送准备/取消准备请求
- `sendStartGame(roomId, seatKey)`：发送开始游戏请求
- `sendResign(roomId, seatKey)`：发送认输请求
- `sendRestart(roomId, seatKey)`：发送重新开始请求
- `sendKick(roomId, targetUserId, seatKey)`：发送踢出玩家请求

#### 17.2.2 Chat WebSocket（chat-service）

**连接地址**：`/chat-service/ws?access_token={token}`

**订阅（接收）**：

| 路径 | 说明 | 消息格式 |
|------|------|----------|
| `/topic/chat.room.{roomId}` | 房间聊天消息（广播） | `{ type: 'ROOM', senderId, senderName, roomId, content, timestamp, clientOpId }` |
| `/user/queue/chat.private` | 私聊消息（点对点） | `{ type: 'PRIVATE', senderId, senderName, targetUserId, content, timestamp, clientOpId }` |
| `/user/queue/notify` | 用户通知（点对点） | `{ id, type, title, content, status, createdAt, actions, payload }` |
| `/user/queue/system.kick` | 系统踢线通知（点对点） | `{ reason: string }` |

**发送**：

| 路径 | 说明 | 请求体 |
|------|------|--------|
| `/app/chat.room.send` | 发送房间消息 | `{ roomId, content, clientOpId }` |
| `/app/chat.private.send` | 发送私聊消息 | `{ targetUserId, content, clientOpId }` |

**工具函数**：
- `removeChatWebSocketCallbacks(callbacks)`：移除回调监听器（不断开连接，只移除指定的回调）
- `subscribeRoomChat(roomId, onEvent)`：订阅房间聊天消息（`/topic/chat.room.{roomId}`），返回取消订阅函数
- `sendRoomChat(roomId, content, clientOpId)`：发送房间消息
- `subscribePrivateChat(onMessage)`：订阅私聊消息（`/user/queue/chat.private`），返回取消订阅函数
- `sendPrivateChat(targetUserId, content, clientOpId)`：发送私聊消息

**内部函数**（不导出，连接时自动调用）：
- `subscribeSystemKick(onKick)`：订阅系统踢线通知（`/user/queue/system.kick`），收到后停止重连
- `subscribeUserNotify(onNotify)`：订阅用户通知（`/user/queue/notify`），用于接收好友申请等通知，同时广播 `gh-notify` 浏览器事件

---

## 总结

Game Hub Web 前端采用现代化的 React 架构，通过双 WebSocket 连接实现游戏逻辑和聊天功能的解耦，采用自动重连和心跳机制保证连接稳定性，使用本地缓存和懒加载优化性能。整体架构清晰，模块化程度高，易于维护和扩展。

**核心亮点**：
1. **双 WebSocket 连接**：游戏和聊天完全独立，互不干扰
2. **完善的重连机制**：指数退避策略，最大 10 次重连
3. **实时通知系统**：WebSocket 推送 + 浏览器事件广播，多组件协同
4. **好友系统**：申请、同意/拒绝、自动刷新好友列表
5. **私聊系统**：已读未读状态、未读计数、历史消息加载
6. **房间聊天**：历史消息加载、实时消息订阅

